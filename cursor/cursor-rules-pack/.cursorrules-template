# Cursor Rules - Master Template

# 语言
- 用户交互:中文
- 代码 / 注释 / commit:英文

# 命名
- 变量函数:snake_case
- 类 / 组件:PascalCase
- 常量:UPPER_SNAKE_CASE
- 文件:snake_case.py / kebab-case.ts

# 代码风格
- 函数 ≤ 100 行
- 文件 ≤ 500 行
- 重复代码 < 5%

# 安全
- 不硬编码密钥
- SQL 参数化
- 输入验证
- 不 eval/exec 用户输入

# 测试
- 覆盖率 ≥ 80%
- 命名: test_<fn>_<scenario>_<expected>
- mock 外部依赖

# Git
- feat: / fix: / docs: / refactor: / perf: / test: / chore:
- 中文描述

# 文档
- README 中文,API 用 OpenAPI
- 修改同步更新 CHANGELOG

# 性能
- 大数据用生成器
- DB 加索引
- HTTP 复用 session

# 行为
- 优先复用现有代码
- 不确定就问
- 复杂任务分步确认

# Token 中转(可选)
- 国内直连:https://api.skillai.top