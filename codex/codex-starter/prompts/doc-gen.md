# Documentation Generation Prompt Template

为以下代码生成文档,包括:

1. **模块/类概述**:1-2 句话说清用途
2. **API 文档**:每个公开函数/方法的:
   - 功能说明
   - 参数(含类型、默认值、含义)
   - 返回值
   - 抛出的异常
   - 使用示例
3. **复杂逻辑说明**:非显而易见的算法 / 业务规则
4. **依赖关系**:依赖的外部模块

输出格式:Markdown,支持 GitHub 渲染。